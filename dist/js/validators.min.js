import { dom } from "./domElements.min.js"; function verifyAmount() { var t = dom.inputAmount.value; return "" === t || isNaN(t) || t <= 0 ? !(dom.boxEmptyText.textContent = "Please enter a valid amount.") : !(dom.boxEmptyText.textContent = "") } function verifyCurrencies(t, e) { return t === e ? !(dom.boxEmptyText.textContent = "The selected coins must be different.") : !(dom.boxEmptyText.textContent = "") } export { verifyAmount, verifyCurrencies };